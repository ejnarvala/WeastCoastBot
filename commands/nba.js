module.exports = {
    name: 'nba',
    description: 'Look up NBA stats',
    args: true,
    usage: `figure it out`,
    async execute(message, args) {
        if (args[0] == 'help') {
            if (args.length > 1) {

            } else {

            }
        }
    }
};